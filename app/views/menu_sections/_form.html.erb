<%= form_with model: menu_section, html: { class: 'space-y-8' } do |f| %>
  <div>
    <%= f.label :name, class: "form-label" %>
    <%= f.text_field :name, class: "form-input", autofocus: true %>
  </div>

  <%= f.submit class: "primary-button" %>
<% end %>