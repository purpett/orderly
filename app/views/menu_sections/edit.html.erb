<div class="mx-auto max-w-sm">
  <h1 class="mt-10 mb-10 text-2xl font-semibold leading-7 text-gray-900">Edit menu section <%= @menu_section.name %></h1>

  <%= render 'form', menu_section: @menu_section %>
</div>